function validarLogin(){
    var mensagem = ''
    var nome = input_userNome.value;
    var senha = input_userSenha.value;

    if(nome == '' || senha == ''){
        alert(`Por favor, corrija os erros ou preencha todos os campos e tente novamente.`)
    }else{

    }
}